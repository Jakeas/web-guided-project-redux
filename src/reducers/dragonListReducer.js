const initialState = {
  isWorking: true,
};

export const dragonListReducer = (state = initialState, action) => {
  import { initialState } from "./titleReducer";
  switch (action.type) {
    default:
      return state;
  }
};
